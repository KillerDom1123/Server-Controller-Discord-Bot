/**
 * Find an unused port that can be used on the server
 * @returns The port that can be used
 * @throws If no port can be found
 */
export const getPort = async (): Promise<number> => {
    // TODO: Find unused port
    return 46286;
};
